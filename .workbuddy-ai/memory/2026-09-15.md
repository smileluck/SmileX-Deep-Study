# 2026-09-15

## /study:plan math-teaching-theory（W7，主题模式 + 全局计划）

- 已有产出（当日早些时候）：`data/topics/math-teaching-theory/plan.md`、`data/plans/master.md`、`data/sessions/20260915-plan-math-teaching-theory.md`。
- 本次复核发现并修正：
  1. **模块篇数错**：原计划按 课标 11 / 教学知识 7 / 教学设计 3 / 案例分析 6 排任务，实际是 **10 / 8 / 3 / 3**（共 24 篇，与 import 一致）。已改 plan.md 的 M2 标准、第 1 周课标与教学知识篇数、第 2 周案例分析篇数，并在现状快照加「模块分布」一行。
  2. **`/api/validate` 报 `type 非法: plan`**：排查确认 `validate.go` 的 `sessionTypes` 白名单**已含** `plan`，报错来自当时监听的陈旧 `make dev` 临时二进制。用当前源码另建二进制跑备用端口 5599 + 同一份 `data/` → `{"ok":true,"errors":[]}`，证明数据合规。已刷新仓库根 `deep-study` 二进制；服务端随后由新一轮 `make dev` 起在当前源码上，validate 已 `ok:true`。
- 数据侧零改动（只改了 plan.md 的篇数与会话日志文字）。
- 复核通过的关键数字：24 笔记 / 76 卡；review-log 覆盖 4/76（5.3%）；due_now 73；mastery level 0（唯一 evidence 是 09-14 import）；Gaps 1 处（`core-literacy-answer-structure` 缺「数据分析」）。
- 最终校验：`{"ok":true,"errors":[],"warnings":[],"checked":{"cards":85,"mastery_topics":2,"materials":1,"notes":28,"plans":2,"sessions":3}}`
