# 2026-09-16

## 复核《高中数学教资-教学设计案例分析模板》导入（W1）

- 材料 3（18.7 KB）于今日 14:32 由上一轮会话完成导入：归档 `library/3-高中数学教资-教学设计案例分析模板.md`，materials.json id=3，topic=math-teaching-theory；新增 8 篇笔记（order 25-32）/ 24 张卡，另补写 4 篇既有笔记（teaching-design-objectives、case-analysis-objectives、case-analysis-process、error-attribution-and-improvement）；会话 `20260916-import-math-teaching-theory.md` 已写，mastery 已追加 import 证据。
- 本轮未重复导入（会重复建 8 笔记 + 24 卡），改为**逐项复核**：
  - 24 张新卡全部通过结构审计：frontmatter 字段齐全、`id` 与文件名一致、`note` 指向正确、`type` 合法、`hint` 均在 15-40 字、`fsrs` 全零且 `due` 带双引号、`last_review: null`；全库 109 张卡 front 无重复。
  - 8 篇新笔记与 4 篇补写笔记逐篇对照材料 3 原文，覆盖完整：教学目标四维/重难点、导入六法四原则、习题设计整体性+针对性、案例评析三段式、教师三角色、学生主体性三条、提问四类型、学生提问处理、错误归因与改进建议三层。
  - 修一处表述不准：`lesson-introduction-methods` 里"导入环节固定结构（三段）"实际列了四步（教师活动→学生活动→教师活动→设计意图），已改为"四步"。
- `/api/validate`：`ok:true`，`errors: []`。

## 用户追问「这是合并到了旧主题吗」→ 确认是 W1 追加导入，非 W8 合并

- 判定证据：topics/ 仍只有 2 个目录；mastery 的 math-teaching-theory 证据 `[import, import]` 无 merge；sessions 无 merge 会话；materials.json 三条归属未动。
- **顺带查出并修掉两处来源标注缺失**：4 篇补写笔记里只有 2 篇标了「材料 3」（case-analysis-process 4 处、teaching-design-objectives 1 处），`case-analysis-objectives` 与 `error-attribution-and-improvement` 标了 0 处，导致材料 2 / 材料 3 的内容分不清。已各补一段「来源标注」。
- **核实方法（可复用）**：把材料 2 PDF 用 pdfplumber 提全文（21 页）存 `/tmp/mat2.txt`，再检索关键词定出处。核对结果：材料 2 的知识原因仅 2 条（无"书写不规范"）、心理原因仅 3 条（无"注意力不够集中"）、无任何题干形式 → 这三项确为材料 3 新增；而"四基、四能"与改进建议三层模板材料 2 早已有，材料 3 只是提炼成"三条总纲"。
- **教训**：给既有笔记补写内容时，必须在正文就近或末尾标出来源材料；否则后续无法追溯哪句来自哪份材料。这条以后写进追加导入的默认动作。

## 发现：5574 上运行的是陈旧构建

- 5574 的 `/api/validate` 长期报 `checked.materials = 1`，而 `/api/materials` 明明返回 3 条。用当前源码另建二进制跑 5599 + 同一份 `data/` 对照，报 `materials = 3`。**结论：5574 是旧构建，`checked` 计数不准；数据本身无问题（两边 errors 均为空）。**
- 复用 2026-09-15 的判定法有效：**怀疑二进制陈旧时，另建二进制跑备用端口 + 同一份 data 对照**。
